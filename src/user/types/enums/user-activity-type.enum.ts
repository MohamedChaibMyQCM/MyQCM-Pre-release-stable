export enum UserActivityType {
  PROFILE_SETUP = "setup_profile", //complete profile setup
  MCQ_ATTEMPT = "mcq_attempt",
  TRAINING_ATTEMPT = "training_attempt",
  OTHERS = "others",
  SUBSCRIPTION = "subscription",
}
